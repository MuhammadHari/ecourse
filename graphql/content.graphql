enum ContentType {
  PDF @enum(value: "pdf")
  VIDEO @enum(value: "video")
}

type Content {
  id: ID!
  sectionId: String!
  classroomId: String!
  title: String!
  description: JSON!
  sequenceNumber: Int!
  type: ContentType!
  createdAt: Date!
  updatedAt: Date
}
